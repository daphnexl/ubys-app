<UserControl x:Class="ubys_app.MVVM.View.CourseSelectionStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1080"
             d:DesignWidth="1920">
    <Grid Background="#2B0A59">
        <Border Background="#F0F0F0"
                Padding="60"
                Margin="100"
                CornerRadius="8">
            <StackPanel>
                <TextBlock Text="COURSE SELECTION STATUS"
                           FontWeight="Bold"
                           FontSize="30"
                           Foreground="Black"
                           Margin="0 0 0 40" />

                <RadioButton GroupName="CourseSelection"
                             Margin="0 0 0 40">
                    <StackPanel>
                        <TextBlock Text="Activate course selection"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#222" />
                        <TextBlock Text="Students can choose courses."
                                   FontSize="20"
                                   Foreground="Gray"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </RadioButton>

                <RadioButton GroupName="CourseSelection">
                    <StackPanel>
                        <TextBlock Text="Disable course selection"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#222" />
                        <TextBlock Text="Students cannot choose courses."
                                   FontSize="20"
                                   Foreground="Gray"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </RadioButton>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
